API_FOOTBALL_KEY=your_api_football_key_here

# OBRIGATÓRIO — mínimo 32 caracteres, string aleatória longa
# Gera uma com: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=MUDA_ISTO_PARA_UMA_STRING_ALEATORIA_LONGA_MIN_32_CHARS
PORT=3000
BASE_URL=http://localhost:3000

# Persistent storage for avatar uploads (point at the Railway Volume mount path)
# Without this, avatars are saved inside the container and lost on every deploy
AVATARS_DIR=/data/avatars

# Resend — envio de emails (recuperação de password)
# https://resend.com → criar conta → API Keys → criar chave
# Se não configurado, o link de reset é mostrado no console (apenas em desenvolvimento)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx
EMAIL_FROM=A Bola Maya <onboarding@resend.dev>

# OAuth — optional, leave blank to disable each provider
# Google: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub: https://github.com/settings/developers → New OAuth App
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

